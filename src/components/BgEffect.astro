---
// Spotlight.astro
---

<div class="spotlight-container">
  <div class="spotlight"></div>
  <slot />
</div>

<style>
  .spotlight-container {
    position: relative;
    background: #0f0f11; /* your dark background */
    overflow: hidden;
    padding: 4rem;
  }

  .spotlight {
    position: absolute;
    top: -20%;
    left: -20%;
    width: 200%;
    height: 200%;
    background: radial-gradient(
      ellipse at center,
      rgba(255, 204, 0, 0.15) 0%,
      rgba(255, 204, 0, 0.03) 60%,
      transparent 100%
    );
    filter: blur(80px);
    pointer-events: none;
    z-index: 0;
    transform: rotate(25deg);
  }

  .spotlight-container > * {
    position: relative;
    z-index: 1;
  }
</style>
